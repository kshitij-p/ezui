import {
  Badge,
  badgeVariants,
  ALL_BADGE_VARIANTS,
  DEFAULT_VARIANTS,
  type BadgeVariants,
  type BadgeProps,
} from "./Badge";

export { Badge, badgeVariants, ALL_BADGE_VARIANTS, DEFAULT_VARIANTS, type BadgeVariants, type BadgeProps };
